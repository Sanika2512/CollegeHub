export function Footer() {
  return (
    <footer className="border-t bg-white">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-4 py-8 text-sm text-slate-500 md:flex-row md:items-center md:justify-between">
        <p>CollegeHub MVP. Built for transparent Indian college discovery.</p>
        <p>Data is seeded for demo and should be verified before admission decisions.</p>
      </div>
    </footer>
  );
}
